CREATE TABLE public.tb_submoduloitem (
	id int8 NOT NULL GENERATED BY DEFAULT AS IDENTITY,
	descricao varchar(50) NOT NULL,
	submodulo_id bigint NOT NULL,
	CONSTRAINT submoduloitem_submodulo_fk FOREIGN KEY (submodulo_id) REFERENCES public.tb_submodulo(id)
);
