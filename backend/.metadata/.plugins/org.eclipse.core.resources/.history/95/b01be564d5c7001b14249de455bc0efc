CREATE TABLE public.tb_submodulo (
	id int8 NOT NULL GENERATED BY DEFAULT AS IDENTITY,
	descricao varchar(50) NOT NULL,
	modulo_id bigint NOT NULL,
	CONSTRAINT submodulo_modulo_fk FOREIGN KEY (modulo_id) REFERENCES public.tb_modulo(id)
);
